# Send all API calls to the proxy worker
# (replace <your-subdomain> if different; you can keep this even if you also added a CF Worker route)
 /api/*   https://permitpulse-proxy.matasergio741.workers.dev/:splat   200

 # SPA-style routes
  /city/*        /city/index.html         200
   /thank-you     /thank-you/index.html    200

   # Fallback to the homepage
    /*            /index.html               200